export enum UserRole {
  ADMIN = 'admin',
}

export enum EmployeeStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  ON_LEAVE = 'on_leave',
}

export enum CompensationType {
  HOURLY = 'hourly',
  SALARY = 'salary',
}
